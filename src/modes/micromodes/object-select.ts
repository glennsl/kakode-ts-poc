/*
export default defineMicroMode('Object Select Mode', [
    defineKey(['b', '(', ')'], 'select enclosing parenthesis', ),
    defineKey(['B', '{', '}'], 'select the enclosing {} block', ),
    defineKey(['r', '[', ']'], 'select the enclosing [] block', ),
    defineKey(['a', '<', '>'], 'select the enclosing <> block', ),
    defineKey(['"', 'Q'], 'select the enclosing double quoted string', ),
    defineKey(["'", 'q'], 'select the enclosing single quoted string', ),
    defineKey(['`', 'g'], 'select the enclosing grave quoted string', ),
    defineKey('w', 'select the whole word', ),
    defineKey('W', 'select the whole WORD', ),
    defineKey('s', 'select the sentence', ),
    defineKey('p', 'select the paragraph', ),
    defineKey(' ', 'select the whitespace', ),
    defineKey('i', 'select the current indentation block', ),
    defineKey('n', 'select the number', ),
    defineKey('u', 'select the argument', ),
    defineKey(':', 'select user defined object, will prompt for oepn and close text', ),
]);
*/